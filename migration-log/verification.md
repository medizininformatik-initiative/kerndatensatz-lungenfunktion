## Verification (generated — do not retype)

Produced by `verify-migration.py` from the target tree AND `migration-log/run.log`, the two oracles. **144 IDENTISCH · 18 DIVERGIERT · 112 NICHT PRÜFBAR.**

Verdicts: **IDENTISCH** = matches the source · **DIVERGIERT** = differs, named below · **NICHT PRÜFBAR** = could not be checked, which is **not** a pass and owes a named human an action.

| Layer | Check | What it asks | IDENTISCH | DIVERGIERT | NICHT PRÜFBAR |
|---|---|---|---|---|---|
| conservation | C1 | every source artefact still exists in the migrated module | 1 | 0 | 0 |
| conservation | C2 | every artefact is reachable from the rendered Artifacts page | 0 | 0 | 1 |
| conservation | C3 | every source guide page was migrated or explicitly retired | 56 | 0 | 0 |
| conservation | C4 | the source's narrative text is present somewhere in the target | 3 | 6 | 47 |
| conservation | C5 | menus lead somewhere, and every page is in a menu | 4 | 0 | 0 |
| conservation | C6 | each text passage landed on the page the page map promised | 44 | 0 | 2 |
| conservation | C7 | content the migration wrote is marked as such in the guide | 0 | 6 | 38 |
| fidelity | F1 | module identity is unchanged (id, canonical, version, licence, ...) | 7 | 2 | 0 |
| fidelity | F2 | dependency versions are pinned exactly as the source pinned them | 1 | 3 | 4 |
| fidelity | F3 | the licence is asserted from evidence, never defaulted | 1 | 1 | 0 |
| fidelity | F4 | no mechanical FSH conversion residue is left | 1 | 0 | 0 |
| provenance | P1 | the rendered site reports the template package it was built with | 0 | 0 | 1 |
| provenance | P2 | the vendored template ref matches what the run log recorded | 0 | 0 | 1 |
| provenance | P3 | the IG Publisher version matches the workflow pin | 0 | 0 | 1 |
| provenance | P4 | the source guide was pinned to a published version, not 'current' | 0 | 0 | 1 |
| provenance | P5 | ig.ini points at the IG resource the build actually writes | 1 | 0 | 0 |
| rendering | R1 | tables, tabs and images render with content, not empty | 0 | 0 | 1 |
| rendering | R2 | page header and footer metadata render correctly | 0 | 0 | 1 |
| rendering | R3 | a translated page really differs from the default language | 0 | 0 | 1 |
| rendering | R4 | no links point at template example artefacts that were deleted | 1 | 0 | 0 |
| rendering | R5 | every page has a title unit in the translation catalogue | 1 | 0 | 0 |
| log | L0 | a run log exists at all | 1 | 0 | 0 |
| log | L1 | every partial-success warning was acted on | 1 | 0 | 0 |
| log | L2 | every expected step actually wrote a log line | 19 | 0 | 10 |
| log | L3 | no identity contradiction is still open | 2 | 0 | 0 |
| log | L4 | the log's counts agree with what the tree holds | 0 | 0 | 3 |

### DIVERGIERT — each one a stop or a recorded decision

| id | Check | What it asks | Subject | Evidence | Next action | Auto-fixable |
|---|---|---|---|---|---|---|
| `C4-811435` | C4 | the source's narrative text is present somewhere in the target | BeschreibungModul.page.md | 4 of 30 PROSE runs of the source page are in no target page (first: Es ist Bestandteil der Erweiterungsmodule des Kerndatensatze…); 6 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-196ae3` | C4 | the source's narrative text is present somewhere in the target | CapabilityStatement.page.md | 4 of 6 PROSE runs of the source page are in no target page (first: ## Um eine dezentrale Datenauswertung mittels des Deutsche F…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-49d110` | C4 | the source's narrative text is present somewhere in the target | FHIR-Profile/Index.page.md | 5 of 5 PROSE runs of the source page are in no target page (first: ## FHIR-Profile Die Arbeiten der Kerndatensatzspezifikatione…); 3 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-1b7347` | C4 | the source's narrative text is present somewhere in the target | MIIIGModulLungenfunktion/Index.page.md | 6 of 16 PROSE runs of the source page are in no target page (first: Die vorliegende Spezifikation beschreibt die FHIR-Repräsenta…); 6 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-fe91db` | C4 | the source's narrative text is present somewhere in the target | Referenzen.page.md | 3 of 5 PROSE runs of the source page are in no target page (first: Er bezieht sich dabei auf Messverfahren, deren Messwerte dur…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-dcd614` | C4 | the source's narrative text is present somewhere in the target | Terminologien.page.md | 1 of 5 PROSE runs of the source page are in no target page (first: //TODO **SNOMED CT** SNOMED CT wird zur Kodierung klinischer…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C7-0209b1` | C7 | content the migration wrote is marked as such in the guide | index | 4 of 30 prose runs of the source page BeschreibungModul.page.md are in no target page (C4), and index carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-9191f0` | C7 | content the migration wrote is marked as such in the guide | capability-statements | 4 of 6 prose runs of the source page CapabilityStatement.page.md are in no target page (C4), and capability-statements carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-8c8c7d` | C7 | content the migration wrote is marked as such in the guide | profiles | 5 of 5 prose runs of the source page FHIR-Profile/Index.page.md are in no target page (C4), and profiles carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-e91ffd` | C7 | content the migration wrote is marked as such in the guide | index | 6 of 16 prose runs of the source page MIIIGModulLungenfunktion/Index.page.md are in no target page (C4), and index carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-283e85` | C7 | content the migration wrote is marked as such in the guide | implementer-guidance | 3 of 5 prose runs of the source page Referenzen.page.md are in no target page (C4), and implementer-guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-583fc9` | C7 | content the migration wrote is marked as such in the guide | value-sets | 1 of 5 prose runs of the source page Terminologien.page.md are in no target page (C4), and value-sets carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `F1-b5b102` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | title | target MII IG Kerndatensatz-Modul Lungenfunktion  vs  source Medizininformatik-Initiative - Kerndatensatz - Modul Lungenf… | the SOURCE wins (spec 2.2); restore it or record the divergence as a Gate-A decision -- never normalise silently | no |
| `F1-7bae24` | F1 | module identity is unchanged (id, canonical, version, licence, ...) | license | source has UNDECLARED, the target declares nothing | carry the source value over unchanged (guardrail 1) | no |
| `F2-d14403` | F2 | dependency versions are pinned exactly as the source pinned them | de.basisprofil.r4 | target 1.5.4  vs  source pin 1.5.x (the source tree (--source)) | the source pin is the evidence; a registry dist-tag is not. Restore the pin or make the bump a Gate-A decision | no |
| `F2-0519f5` | F2 | dependency versions are pinned exactly as the source pinned them | de.medizininformatikinitiative.kerndatensatz.base | target 2026.0.1  vs  source pin 2026.0.x (the source tree (--source)) | the source pin is the evidence; a registry dist-tag is not. Restore the pin or make the bump a Gate-A decision | no |
| `F2-ce5cda` | F2 | dependency versions are pinned exactly as the source pinned them | de.medizininformatikinitiative.kerndatensatz.meta | target 2026.0.0  vs  source pin 2026.0.x (the source tree (--source)) | the source pin is the evidence; a registry dist-tag is not. Restore the pin or make the bump a Gate-A decision | no |
| `F3-1a29c4` | F3 | the licence is asserted from evidence, never defaulted | license | the target declares no licence at all | declare the source's licence; a missing licence is not a default | no |

### NICHT PRÜFBAR — not a pass; each needs a human

| id | Check | What it asks | Subject | Why not mechanisable | Who does what |
|---|---|---|---|---|---|
| `C2-bd3456` | C2 | every artefact is reachable from the rendered Artifacts page | artifact index | no rendered output with an artifacts.html under output | build the IG (step 7), then re-run verification |
| `C4-7b2767` | C4 | the source's narrative text is present somewhere in the target | AnwendungsflleInformationsmodell/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-14eb4f` | C4 | the source's narrative text is present somewhere in the target | BF.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-b3ff9c` | C4 | the source's narrative text is present somewhere in the target | Bodyplethysmographie/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-83a2af` | C4 | the source's narrative text is present somewhere in the target | Bodyplethysmographie/Procedure.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-24bf74` | C4 | the source's narrative text is present somewhere in the target | Bodyplethysmographie/Report.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-7cc045` | C4 | the source's narrative text is present somewhere in the target | CO2-Konzentration.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-867830` | C4 | the source's narrative text is present somewhere in the target | DLCO.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-97d57a` | C4 | the source's narrative text is present somewhere in the target | DLCOc.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-22d4dc` | C4 | the source's narrative text is present somewhere in the target | Diffusion/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-b97b9f` | C4 | the source's narrative text is present somewhere in the target | Diffusion/Procedure.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-925b80` | C4 | the source's narrative text is present somewhere in the target | Diffusion/Report.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-6eb06a` | C4 | the source's narrative text is present somewhere in the target | Dosis.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-efbe04` | C4 | the source's narrative text is present somewhere in the target | FEV.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-3cb76c` | C4 | the source's narrative text is present somewhere in the target | FEVFVC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-81707e` | C4 | the source's narrative text is present somewhere in the target | FRC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-ec5bd7` | C4 | the source's narrative text is present somewhere in the target | FVC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-7aef82` | C4 | the source's narrative text is present somewhere in the target | Hb.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-5c36ef` | C4 | the source's narrative text is present somewhere in the target | IC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-fc2931` | C4 | the source's narrative text is present somewhere in the target | KCO.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e0860e` | C4 | the source's narrative text is present somewhere in the target | KCOc.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 5 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-98b004` | C4 | the source's narrative text is present somewhere in the target | Luftfeuchtigkeit.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-49a45d` | C4 | the source's narrative text is present somewhere in the target | Lufttemperatur.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-bc8e28` | C4 | the source's narrative text is present somewhere in the target | MEF.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 5 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-3884b9` | C4 | the source's narrative text is present somewhere in the target | Medikation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-37823d` | C4 | the source's narrative text is present somewhere in the target | PEF.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-fb68e8` | C4 | the source's narrative text is present somewhere in the target | Provokationsgabe.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-16bc5c` | C4 | the source's narrative text is present somewhere in the target | Provokationstest/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-4e547c` | C4 | the source's narrative text is present somewhere in the target | Provokationstest/Procedure.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-3b309f` | C4 | the source's narrative text is present somewhere in the target | Provokationstest/Report.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-1492bc` | C4 | the source's narrative text is present somewhere in the target | RV.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-eea74e` | C4 | the source's narrative text is present somewhere in the target | RVL.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-ca3565` | C4 | the source's narrative text is present somewhere in the target | RVLTLC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-cb0eec` | C4 | the source's narrative text is present somewhere in the target | R_tot.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-03ec0a` | C4 | the source's narrative text is present somewhere in the target | Release-Notes.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-8056fc` | C4 | the source's narrative text is present somewhere in the target | Spirometrie/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-0e259f` | C4 | the source's narrative text is present somewhere in the target | Spirometrie/Procedure.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-841864` | C4 | the source's narrative text is present somewhere in the target | Spirometrie/Report.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-2ac857` | C4 | the source's narrative text is present somewhere in the target | TLC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-fc1144` | C4 | the source's narrative text is present somewhere in the target | TechnischeImplementierung/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-d47516` | C4 | the source's narrative text is present somewhere in the target | UML.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-8e4c1e` | C4 | the source's narrative text is present somewhere in the target | Umgebung.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-580dd4` | C4 | the source's narrative text is present somewhere in the target | Umgebungsvariablen/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-05ce18` | C4 | the source's narrative text is present somewhere in the target | VA.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-bdec68` | C4 | the source's narrative text is present somewhere in the target | VC.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-9dadbd` | C4 | the source's narrative text is present somewhere in the target | sG_tot.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-1ad1d6` | C4 | the source's narrative text is present somewhere in the target | sR_eff.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-9ba3b4` | C4 | the source's narrative text is present somewhere in the target | sR_tot.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 2 of 3 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C6-56cdd3` | C6 | each text passage landed on the page the page map promised | BeschreibungModul.page.md | mapped to index, which holds 4 run(s), but MOST landed on guidance (20), index (4), implementer-guidance (2) | a split is legitimate when the source page was deliberately divided; confirm the routing or correct the page map |
| `C6-02fd5c` | C6 | each text passage landed on the page the page map promised | RVL.page.md | mapped to StructureDefinition-mii-pr-lungenfunktion-rvl-intro, which holds 1 run(s), but MOST landed on StructureDefinition-mii-pr-lungenfunktion-irv-intro (1), StructureDefinition-mii-pr-lungenfunktion-rvl-intro (1) | a split is legitimate when the source page was deliberately divided; confirm the routing or correct the page map |
| `C7-9c179f` | C7 | content the migration wrote is marked as such in the guide | BF.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-223ee1` | C7 | content the migration wrote is marked as such in the guide | Bodyplethysmographie/Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-75d603` | C7 | content the migration wrote is marked as such in the guide | Bodyplethysmographie/Report.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-5fb496` | C7 | content the migration wrote is marked as such in the guide | CO2-Konzentration.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-e2ffb7` | C7 | content the migration wrote is marked as such in the guide | DLCO.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-753e86` | C7 | content the migration wrote is marked as such in the guide | DLCOc.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-d2515d` | C7 | content the migration wrote is marked as such in the guide | Diffusion/Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-59f32e` | C7 | content the migration wrote is marked as such in the guide | Diffusion/Report.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-2b0320` | C7 | content the migration wrote is marked as such in the guide | Dosis.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-91cda2` | C7 | content the migration wrote is marked as such in the guide | FEV.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-bee0bc` | C7 | content the migration wrote is marked as such in the guide | FEVFVC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-88f5cf` | C7 | content the migration wrote is marked as such in the guide | FRC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-c80000` | C7 | content the migration wrote is marked as such in the guide | FVC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-3ff806` | C7 | content the migration wrote is marked as such in the guide | Hb.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-70d5a8` | C7 | content the migration wrote is marked as such in the guide | IC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-b7cc8e` | C7 | content the migration wrote is marked as such in the guide | KCO.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-b0edc9` | C7 | content the migration wrote is marked as such in the guide | KCOc.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-c0ee09` | C7 | content the migration wrote is marked as such in the guide | Luftfeuchtigkeit.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-8aa92f` | C7 | content the migration wrote is marked as such in the guide | Lufttemperatur.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-9fab0e` | C7 | content the migration wrote is marked as such in the guide | MEF.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-9fa9da` | C7 | content the migration wrote is marked as such in the guide | Medikation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-7eb05e` | C7 | content the migration wrote is marked as such in the guide | PEF.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-cfe957` | C7 | content the migration wrote is marked as such in the guide | Provokationsgabe.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ba8f04` | C7 | content the migration wrote is marked as such in the guide | Provokationstest/Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-de34f1` | C7 | content the migration wrote is marked as such in the guide | Provokationstest/Report.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-51b639` | C7 | content the migration wrote is marked as such in the guide | RV.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-682394` | C7 | content the migration wrote is marked as such in the guide | RVL.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ef7d23` | C7 | content the migration wrote is marked as such in the guide | RVLTLC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-6f91c0` | C7 | content the migration wrote is marked as such in the guide | R_tot.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-0599a5` | C7 | content the migration wrote is marked as such in the guide | Spirometrie/Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ea828e` | C7 | content the migration wrote is marked as such in the guide | Spirometrie/Report.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-2ef662` | C7 | content the migration wrote is marked as such in the guide | TLC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ca6f8b` | C7 | content the migration wrote is marked as such in the guide | Umgebung.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-85e405` | C7 | content the migration wrote is marked as such in the guide | VA.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-8f215a` | C7 | content the migration wrote is marked as such in the guide | VC.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-7e377c` | C7 | content the migration wrote is marked as such in the guide | sG_tot.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-87f2aa` | C7 | content the migration wrote is marked as such in the guide | sR_eff.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-97df42` | C7 | content the migration wrote is marked as such in the guide | sR_tot.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `F2-fb1c14` | F2 | dependency versions are pinned exactly as the source pinned them | de.medizininformatikinitiative.kerndatensatz.medikation | target-only dependency 2026.0.1 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-fd0e73` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.crmi | target-only dependency 2.0.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-047205` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.extensions.r4 | target-only dependency 5.3.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-8f9355` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.terminology.r4 | target-only dependency 7.3.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `P1-8d8889` | P1 | the rendered site reports the template package it was built with | rendered template version | no `Templates:` line in the rendered qa.html (qa.html not found) | build the IG (step 7); the rendered output is the only place this value can be READ rather than assumed |
| `P2-264584` | P2 | the vendored template ref matches what the run log recorded | vendored template ref | no `5.2 skeleton-vendored … ref=` line in the run log | emit it when vendoring: `bash "$ML" info 5.2 skeleton-vendored "… ref=<tag> commit=<sha>"` |
| `P3-a28a47` | P3 | the IG Publisher version matches the workflow pin | IG Publisher version | no `IG Publisher Version:` in the rendered qa output | build the IG (step 7) |
| `P4-379bde` | P4 | the source guide was pinned to a published version, not 'current' | source guide version | no `?version=` recorded in the run log or the harvest manifest | record the pinned, PUBLISHED guide version like the source commit SHA (spec 5.1c.3) |
| `R1-32a7b3` | R1 | tables, tabs and images render with content, not empty | rendered output | no built site under output | build the IG (step 7); rendering integrity is not a property of the sources |
| `R2-9639ec` | R2 | page header and footer metadata render correctly | rendered output | no built site under output | build the IG (step 7); rendering integrity is not a property of the sources |
| `R3-f02a27` | R3 | a translated page really differs from the default language | rendered output | no built site under output | build the IG (step 7); rendering integrity is not a property of the sources |
| `L2-23b069` | L2 | every expected step actually wrote a log line | 2.1 package-identity | no line in the log; the step is conditional (no sushi-config.yaml in the source) | confirm the condition did not hold -- The published package is the only machine source for the dependency PINS (spec §2.1.1). |
| `L2-7163a2` | L2 | every expected step actually wrote a log line | 2.1 repo-identity | no line in the log; the step is conditional (no sushi-config.yaml in the source) | confirm the condition did not hold -- The repository's LICENSE text is the only machine source for `license` — the field that must never default (spec §2.2). |
| `L2-07bdbb` | L2 | every expected step actually wrote a log line | 5.1c simplifier-discover | no line in the log; the step is conditional (no rendered-IG URL was supplied) | confirm the condition did not hold -- Without the discovery chain the guide is not found, and a migration then ships the template's starter pages. |
| `L2-930ba2` | L2 | every expected step actually wrote a log line | 5.1d guide-harvest | no line in the log; the step is conditional (the narrative is not in the repository) | confirm the condition did not hold -- This is the step whose absence shipped the template's starter pages under a module's name. |
| `L2-91b7ea` | L2 | every expected step actually wrote a log line | 5.1b.2 gofsh-input | no line in the log; required only for source shape B, which the log does not state | record the shape (pre.2 classify-source-shape) -- The input count, taken BY CONTENT, is the number goFSH's own counts are reconciled against. |
| `L2-f0a960` | L2 | every expected step actually wrote a log line | 5.1b.2 gofsh-convert | no line in the log; required only for source shape B, which the log does not state | record the shape (pre.2 classify-source-shape) -- goFSH exits 0 having converted 1 of 20 inputs when `-t json-and-xml` is missing; only this line's counts show it. |
| `L2-395531` | L2 | every expected step actually wrote a log line | 5.1b.3 sushi-before | no line in the log; required only for source shape B, which the log does not state | record the shape (pre.2 classify-source-shape) -- The "41" of 41 → 5: without the baseline the repair's effect is unmeasurable. |
| `L2-837623` | L2 | every expected step actually wrote a log line | 5.1b.3 postprocess-gofsh | no line in the log; required only for source shape B, which the log does not state | record the shape (pre.2 classify-source-shape) -- The two mechanical repairs; a parse error otherwise TRUNCATES a file while SUSHI still exports the instance. |
| `L2-41942e` | L2 | every expected step actually wrote a log line | 5.1b.3 sushi-after | no line in the log; required only for source shape B, which the log does not state | record the shape (pre.2 classify-source-shape) -- The "5": the residual, which must be named unresolvable parents and nothing else. |
| `L2-cd2674` | L2 | every expected step actually wrote a log line | 5.1b.5 parent-snapshots | no line in the log; the step is conditional (a parent package ships no snapshots) | confirm the condition did not hold -- A hand-rolled snapshot fabricates a parent; the detect run is what proves none was needed. |
| `L4-106e61` | L4 | the log's counts agree with what the tree holds | conversion count | no `gofsh-convert … actual=` line in the log | shape B only; for shape A there is nothing to convert |
| `L4-f17740` | L4 | the log's counts agree with what the tree holds | page count | no harvested count in the log and/or no harvest manifest | harvest the guide (step 2c) where the narrative is not in the repo |
| `L4-40cf60` | L4 | the log's counts agree with what the tree holds | artifact count | no rendered artifacts.html to count | build the IG (step 7) |

**Inputs:** target `.` · source `/private/tmp/claude-501/-Users-thome-code-fhir-profiling-kerndatensatz-lungenfunktion/e51118b5-2071-4f11-b0c1-94b9697c5cb5/scratchpad/source-main` · rendered `/Users/thome/code/fhir-profiling/kerndatensatz-lungenfunktion/output` · log `./migration-log/run.log`
